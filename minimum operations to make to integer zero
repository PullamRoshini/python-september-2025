class Solution:
    def makeTheIntegerZero(self, num1: int, num2: int) -> int:
        for k in range(1,61):
            x=num1 -k*num2
            if x>=0 and x.bit_count()<=k and x>=k:
                return k
        return -1
        
