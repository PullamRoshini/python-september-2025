class Solution:
    def getNoZeroIntegers(self, n: int) -> List[int]:
        def is_valid(n):
            while n>0:
                d=n%10
                if d==0:
                    return False
                n=n//10
            return True
        for a in range(1,n):
            b=n-a
            if is_valid(a)==True and is_valid(b)==True:
                return [a,b]
                break
        
